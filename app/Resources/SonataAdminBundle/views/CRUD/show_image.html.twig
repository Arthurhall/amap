{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}

{% if value %}

{% set path = '/' ~ value %}

<img src="{{ path | apply_filter('my_thumb') }}" border="0" alt="{{object.alt}}" title="{{object.title}}" />
{% endif %}

{% endblock %}